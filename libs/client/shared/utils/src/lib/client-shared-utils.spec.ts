import { clientSharedUtils } from './client-shared-utils';

describe('clientSharedUtils', () => {
  it('should work', () => {
    expect(clientSharedUtils()).toEqual('client-shared-utils');
  });
});
